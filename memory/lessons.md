# Lessons Learned

Format: `[date] | what went wrong | rule to prevent it`. Review at every session start.

2026-06-01 | Spent a long detour evaluating and building the CRM on self-hosted **Twenty**, only to hit the wall that Twenty needs a 24/7 Docker server (WSL + admin + reboot + ongoing cost) — the opposite of the user's serverless model and unrunnable on their machine. | For this user, default to the **serverless stack (Convex/Clerk/TanStack/Cloudflare)**. Don't propose self-hosted server apps (Docker/VPS) for a CRM unless they explicitly say they want to run a server. Surface the hosting requirement (server vs serverless) BEFORE building.

2026-06-01 | Convex **circular type inference**: action handlers that reference sibling functions in the same file (via `internal.*`, `ctx.runAction`, scheduled fan-out) failed with `TS7022/TS7023` ("implicitly has type any"). | Add an **explicit return-type annotation** (`: Promise<...>`) to every Convex action handler that calls other functions. Define a small result type and annotate.

2026-06-01 | Tried to put queries/mutations and a `"use node"` action in the same Convex file → push failed. | A `"use node"` file can ONLY contain actions. **Split**: V8 queries/mutations in `xData.ts`, `"use node"` actions in `xActions.ts`.

2026-06-01 | `convex dev` refused to push: "Environment variable CLERK_JWT_ISSUER_DOMAIN is used in auth config file but its value was not set" — even though it was in a dead conditional branch. | Convex **statically requires every env var referenced in `auth.config.ts`** to exist. Set it (a dummy locally, e.g. `https://dev.clerk.accounts.dev`) so dev pushes before Clerk is wired.

2026-06-01 | Assumed a live backend test was impossible without Docker/cloud login. It wasn't. | **Convex runs locally with NO account/Docker/admin** via `CONVEX_AGENT_MODE=anonymous npx convex dev` (downloads a user-space backend binary). Use it for offline end-to-end testing. (Later switched to the user's real dev deploy key via `CONVEX_DEPLOY_KEY` in `.env.local`.)

2026-06-01 | The sale-period label (`saleMonth`) was derived from `new Date()`, so a scheduled run before the county posts the new PDF would mislabel last month's data and the idempotency guard would lock the month as "done". | **Derive period labels from the source document**, not `now`. The sheriff PDF carries `Gross List MM/DD/YYYY`; parse it. Idempotency keys must come from the real data.

2026-06-01 | A helper that built object fields widened enum literals (e.g. `FieldType.TEXT` → `FieldType`), breaking a discriminated-union schema type. | When an object must match a **discriminated union**, build it inline (or annotate) so the literal `type` is preserved; don't pass it through a generic helper that widens.

2026-06-01 | Node 24 + Convex CLI on Windows prints `Assertion failed: !(handle->flags & UV_HANDLE_CLOSING)` and exits non-zero. | It's a **cosmetic teardown crash** that fires AFTER the command's real output. Trust the captured output, not the exit code, for `convex` CLI calls here.

2026-06-01 | A `git commit` with a PowerShell here-string broke when the message contained `->`, quotes, and parens (parsed as separate pathspecs). | On Windows PowerShell, use **simple single-line `-m` commit messages**; avoid here-strings with shell-special characters.

2026-06-01 | The scrape run was created AFTER fetch+parse, so a fetch failure or an idempotency skip recorded nothing and the UI button silently did nothing; worse, a mid-run throw left the run `status:"running"` forever, which permanently locks the `startScrape` concurrency guard. | **Create the run first (phase "starting") and always finalize it in try/catch** (`finishRun` complete/failed). Record skips as a visible event. A run must never be left "running" or the button locks.

2026-06-01 | Passing a Convex mutation call directly as an `onEvent` callback typed `(e) => void | Promise<void>` failed `TS2345`: `ctx.runMutation(...)` returns `Promise<Id>`, not `Promise<void>`. | When a Convex mutation/action is used as a void-returning callback, **wrap it**: `async (e) => { await ctx.runMutation(...); }`. Don't return the mutation promise directly.

2026-06-01 | Integrating a shadcn component (`stepper.tsx`) into this non-shadcn Tailwind-v4 project. The vendor's "install these deps" list (Button, radix-ui, lucide-react, cva) was mostly wrong — `stepper.tsx` imports ONLY `cn`. | **Check the component's actual imports, don't trust the dep list.** It usually needs only `cn` (`clsx` + `tailwind-merge`). Setup = add `@/`→`src` alias (vite `resolve.alias` + tsconfig `paths`), create `src/lib/utils.ts` + `src/components/ui/`, and add the shadcn semantic tokens (`--color-primary` etc.) to the `@theme` block in `index.css` WITHOUT clobbering existing tokens. In Tailwind v4 an undefined token's class simply isn't generated — **no build error**, just no style, so missing tokens degrade gracefully.

2026-06-01 | `convex dev --once` runs `tsc` on the convex/ functions and refuses to push/regenerate `_generated` types on a type error — and the frontend `tsc` then reads stale generated types for any newly added/renamed Convex function. | After adding/changing Convex functions, **run `npx convex dev --once` first** (it validates convex/ AND regenerates `_generated`), then run the frontend `npm run build`. Don't run the frontend typecheck expecting new Convex API symbols before the codegen.

2026-06-01 | ~30% of parcel rows came back `SCRAPE FAILED` at scale. Root cause: NCC's Reblaze bot protection returns a **block page with HTTP 200** (not an HTTP error), so `firecrawlScrape`'s built-in HTTP retry never fires; `lookupParcel` then threw on the content check with no retry. | For sites with bot protection, **retry at the operation level, not just the HTTP level**: wrap the whole browser-action sequence in `withRetry` and retry when the *content* check fails (block page), since a re-attempt usually clears Reblaze. Also throttle peak concurrency (bigger scheduler stagger) — `runAfter(i*ms)` staggers starts but they still overlap, so volume still trips rate limits.

2026-06-01 | Sheriff-sale "cushion" math is sale-type-dependent and easy to get backwards. | **TAX (monition) sale:** the foreclosing `principal` ≈ the delinquent county/school/sewer taxes, so cost ≈ principal — do NOT also add the balances (double-count). **MTG (Lev Fac)/JUDG sale:** principal is the loan and the taxes are SEPARATE senior liens, so cost ≈ principal + balances; watch for a surviving senior mortgage if principal is tiny vs. value. Logic lives in `src/scraper/deal.ts` (`computeDeal`), unit-tested, and runs server-side in `sheriffData.monthListings`. (Delaware Code: taxes are paid from sale proceeds by priority; sheriff says bidder must research title per property — not legal advice.)

2026-06-01 | Cloudflare build failed: `tsc` couldn't resolve `convex/_generated/api|dataModel` (TS2307) + a cascade of implicit-`any` (TS7006), because `convex/_generated` was in `.gitignore` — so it never reached GitHub/Cloudflare's clean checkout. It exists locally, so `npm run build` passed locally and masked it. | **Commit `convex/_generated` to version control** (Convex's documented best practice: CI typechecks without running the Convex CLI). `convex codegen` may need to talk to a deployment, so don't rely on a codegen step in CI. Verify CI-type build from a fresh `git clone` (not the working tree, which carries untracked files). And remember **build passing ≠ app working**: the deployed frontend still needs `VITE_CONVEX_URL` (+ `VITE_GOOGLE_MAPS_API_KEY`/`_MAP_ID`) set in Cloudflare's build env to connect at runtime.

2026-06-01 | A map's "fit to all pins" `useEffect` depended on the `points` array, which a Convex/React query rebuilds as a NEW reference every render — so it re-fit (snapping the map back to full bounds) on every reactive update, e.g. editing a deal status from a pin. | When a `useEffect` should fire only on *content* change, don't depend on an array/object that's rebuilt each render. Key it on a **stable signature** (e.g. `points.map(p => p.id+p.lat+p.lng).join("|")`), not the reference.

2026-06-01 | Tried to reuse the one Google Maps **browser** key for server-side Geocoding (in a Convex action). A browser key restricted by **HTTP referrer** is rejected server-side (a server request has no referrer); an *unrestricted* key that does work server-side is a billing-theft risk if exposed. | Google Maps needs **two keys**: a referrer-restricted browser key (`VITE_GOOGLE_MAPS_API_KEY`, Maps JS + Street View Static) and a separate server key restricted to the Geocoding API (`GOOGLE_GEOCODING_API_KEY`, Convex env). Also: `AdvancedMarker` requires a Map ID (`DEMO_MAP_ID` works for dev).

2026-06-01 | Clerk's docs offer a **"TanStack React Start"** quickstart (an SSR meta-framework: `clerkMiddleware`, `createServerFn`, `src/start.ts`, `__root.tsx` shell, `@clerk/tanstack-react-start`). This project is a **Vite SPA** (TanStack *Router*, client-only) with a Convex backend on static Cloudflare. Following that quickstart would be an unasked SSR rewrite that breaks scales-to-zero AND wouldn't wire Convex auth at all. | For **Vite + React + Convex**, use `@clerk/clerk-react` (already installed) + `ConvexProviderWithClerk` from `convex/react-clerk`: `<ClerkProvider publishableKey>` → `<ConvexProviderWithClerk client useAuth>` → `<Authenticated>`/`<Unauthenticated>`/`<AuthLoading>`. `auth.config.ts` needs `{ domain: CLERK_JWT_ISSUER_DOMAIN, applicationID: "convex" }` AND a Clerk **JWT template named `convex`** (dashboard → JWT Templates → Convex) or tokens never authenticate. The publishable key encodes the issuer domain (base64 after `pk_test_` → `optimal-frog-32.clerk.accounts.dev`). **`IRES_DEV=1` masks a broken token path** (`requireUser` returns `"dev-user"` for ANY caller) — so "signed in + app loads" proves nothing; verify with a temp `whoami` query (expect `user_…`, not `dev-user`) or by removing `IRES_DEV` on dev. Don't `throw` at module load on a missing `VITE_CLERK_PUBLISHABLE_KEY` (breaks the Cloudflare build); guard at render.

2026-06-01 | I asserted "there's no official Clerk CLI" from memory — wrong. Clerk has a CLI (https://clerk.com/cli): `clerk init` (detect framework, link app, install packages), `clerk config` (manage auth settings as code), `clerk deploy` (prod: domain setup, OAuth providers, DNS records). | Don't claim an external tool doesn't exist based on training memory — **verify** (the user corrected me). For this project: the Clerk CLI does NOT manage JWT templates (use the Backend API `POST https://api.clerk.com/v1/jwt_templates` with the `sk_` key, or the dashboard), but `clerk deploy` will be the clean path for the **prod** Clerk instance (domain/OAuth/DNS). The `convex` JWT template was created via the Backend API: name `convex`, claims `{"aud":"convex"}`, RS256.

2026-06-02 | A Google Maps key restricted to **HTTP referrers (Websites)** still works for the **Geocoding web service called server-side** (Google doesn't enforce the referrer when there's no `Referer` header) — confirmed by curling the Geocoding API with the prod browser key (`status: OK`) and by BlueRock's single-key setup. | **One domain-restricted key can serve BOTH** the browser map (`VITE_GOOGLE_MAPS_API_KEY`) and server geocoding (`GOOGLE_GEOCODING_API_KEY` = same value) — **supersedes the 2026-06-01 "two keys required" lesson**. Enable on the key: **Maps JavaScript API + Geocoding API + Street View Static API** (NOT Places/Directions — those are BlueRock's). IRES geocoding had failed only because `GOOGLE_GEOCODING_API_KEY` held an **expired** key value (`REQUEST_DENIED: "The provided API key is expired"`). Diagnose any geocode failure by curling `https://maps.googleapis.com/maps/api/geocode/json?address=…&key=…` directly.

2026-06-02 | `backfillGeocodes`' `catch {}` swallowed the expired-key error with no log → the "Geocode N missing" button appeared to "do nothing" (returned `{geocoded:0, failed:0}` while `listMissing` returned 53 un-geocoded rows), with zero feedback. | A blanket `catch {}` meant for *transient* retries also hides *hard config* errors (REQUEST_DENIED, expired/invalid key). **Log + distinguish hard vs transient** — a dead key should surface, not silently no-op forever. (Geocoded coords are stored per row, so they persist across key changes; re-geocoding only touches still-missing rows.) [TODO: fix the silent catch in `convex/geocodeActions.ts`.]

2026-06-02 | ires-crm's Cloudflare **Workers** build "succeeded" but the domain kept serving a stale bundle (hash never changed; no `VITE_CONVEX_URL` → "No address provided to ConvexReactClient"). Root cause: no `wrangler.jsonc`, so `npx wrangler deploy` didn't know to serve `./dist`. | A Cloudflare **Workers** project serving a static Vite SPA needs a committed root **`wrangler.jsonc`**: `{ name, compatibility_date, compatibility_flags:["nodejs_compat"], assets:{ directory:"./dist", not_found_handling:"single-page-application" } }`. **`name` MUST equal the Workers project name** (`instant-real-estate-solution-crm`) or it deploys to the wrong/new worker. Mirror BlueRock/PeakWeb. (Cloudflare **Pages** does this automatically; Workers doesn't.)

2026-06-02 | Cloudflare **Workers** build command `npx convex deploy --cmd 'npm run build'` failed: *"Detected a non-production build environment and CONVEX_DEPLOY_KEY for a production Convex deployment."* Convex auto-detects Vercel/Netlify (and CF Pages via `CF_PAGES`) as production, but not the Workers build env. | Two working options: (a) **plain `npm run build`** on Cloudflare + set `VITE_CONVEX_URL` explicitly + deploy the backend **manually** with `npx convex deploy` (what IRES uses); or (b) BlueRock's wrapper-on-Workers, which works with **`NODE_VERSION=22`** in CF env + build cmd `… npx convex deploy --cmd 'pnpm build'`. Backend-deploy-on-git-push is optional, not required.

2026-06-02 | Standing up the Clerk **production** instance for a private/invite-only CRM. | Clerk **Restrictions → "Enable restricted mode"** = sign-ups disabled, only invited/manually-created users get in (the private-CRM lock) — but keep **"Sign-up with email" ON** or Clerk rejects invitations with `invitations_not_supported` ("only supported on instances that accept email addresses"). Bootstrap the **first admin** via Clerk dashboard **Users → Create user** (admin-created bypasses restricted mode), then **sign in** (not up). Recreate the `convex` **JWT template** (with the `email` claim) on the prod instance. `CLERK_SECRET_KEY=sk_live` must be on Convex prod for invite/deactivate/delete (sign-in itself works without it, via JWKS verification of the issuer).

2026-06-03 | Built the shadcn UI foundation on this Tailwind-v4 Vite SPA. | (a) `npx shadcn@latest init` still PROMPTS for a style preset even with `--yes` — run non-interactively: `--base radix --preset nova --template vite --yes` (Nova = Lucide/Geist, matches our lucide icons + Geist). It APPENDS the canonical theme (`@theme inline` + `:root`/`.dark` + base layer) to `src/web/index.css` WITHOUT clobbering the existing hand-rolled `@theme`. (b) But that creates **token-NAME collisions**: IRES had overloaded `--color-accent` = brand green, while shadcn defines `--color-accent` = neutral hover; the later `@theme inline` wins, so `bg-accent`/`text-accent` **silently turn neutral** (Tailwind v4 overrides with NO build error). Fix: let shadcn own the semantic tokens, set `--primary` = IRES green (`#16a34a`) so `bg-primary` is green app-wide, keep only non-colliding brand tokens (`--color-ink`), set the navy `--sidebar*` + IRES `--chart-1..5`, and migrate existing brand `accent`→`primary` classes (leave true shadcn-neutral `accent` like the stepper). Verify by GREPPING the brand-class usages + an actual screenshot, not a green build.

2026-06-03 | Installed the `@efferd/dashboard-3` block. | It cascades `@efferd/app-shell-3` (full sidebar+header) + ~12 shadcn base components (sidebar/card/chart/table/…) + recharts. The efferd components use radix `asChild` → MUST init with `base: radix` (verify by fetching a block file's source and checking `asChild` vs `render` BEFORE installing). Pin the registry to a path verified to serve radix: `"@efferd": "https://efferd.com/r/new-york/{name}.json"`. One block file targeted `~/components/...` → the CLI wrote it to the **repo-root `components/`** (not `src/components/`) while the import expected `@/components/...`; relocate it. Always `add --dry-run` first + review every added file; swap the icon lib if needed (here it's already lucide).

2026-06-03 | Visual-verifying an auth-gated SPA (Clerk) headlessly with no credentials. | The sign-in gate is the only thing renderable unauthenticated; it does NOT exercise the authed shell/dashboard. To see those: COMMIT first (durable), then temporarily (a) mock the data hooks (`useQuery`/`useUser`/`useClerk`) inline, (b) add a throwaway `preview.html` + entry using TanStack `createMemoryHistory({ initialEntries: ["/"] })` so the router matches a real route (the browser URL `/preview.html` → "Not Found"), and (c) screenshot via headless Chrome (`"…/chrome.exe" --headless=new --virtual-time-budget=9000 --screenshot=OUT.png URL`). Then `git checkout -- <files>` to revert (safe because committed). **recharts bars/pie don't render in the headless snapshot** (entry animation under virtual-time) — set `isAnimationActive={false}` only for the screenshot. Chrome + Edge are installed; Playwright is not.

2026-06-03 | "Use the logo from the IRES website in the desktop directory." | There was NO clean logo asset on the Desktop — the `InstantRealEstateSolutionsMarketing` PNGs show a **broken-image placeholder** + the "Instant Real Estate Solutions" wordmark. The real logo lives on the **live site**: downloaded `logoForBlueBackground` (navy-bg variant, for the navy sidebar) + the light variant from `instantrealestatesolution.com/assets/` → `public/ires-logo-onnavy.png` / `ires-logo-dark.png`, plus a cropped square `ires-icon.png` for the collapsed sidebar. Lesson: when a referenced local asset doesn't exist, check the live site before recreating; confirm the logo reads on its actual background.

2026-06-03 | Re-themed the whole app to a dark "Industrial Precision" look (deep black + teal frames + metallic-yellow CTAs + Inter + 3% noise grain). | Gotchas: (1) Tailwind v4's `@custom-variant dark (&:is(.dark *))` means `dark:` utilities only fire under a `.dark` ancestor — drive the theme with **`class="dark"` on `<html>` + the palette in the `.dark` block**, NOT by editing `:root` only (else shadcn/component `dark:` literals fall back to LIGHT on a black page → invisible elements). (2) Keep a 3-level surface hierarchy: `--background #0a0a0a` < `--card #161616` < `--muted #1c1c1c`; if `--muted == --card`, `bg-muted` fills (table headers, segmented controls) blend into cards and look "funky" (this was the Admin-page bug). (3) `--primary` = yellow has a ripple: the earlier brand-`accent→primary` migration also turned tabs/links/focus yellow — SPLIT it: **teal** for active-tab / links / focus (custom `--color-teal` + `--color-teal-glow` → `text-teal-glow`/`border-teal`/`bg-teal`), **yellow** only for CTAs; and `bg-primary text-white` is white-on-yellow (unreadable) → route CTAs through a `.btn-metal-yellow` component class (gradient + dark text + hover lift). (4) Signature card frame: a single **unlayered** `.dark [data-slot="card"]` rule (1.5px teal ~30% + heavy shadow + inset glow + `fadeInUp`) frames every shadcn Card without editing the vendored component; keep `--border` NEUTRAL so inputs/dividers/table rows aren't all teal. (5) Noise = a fixed `body::after` with an inline `feTurbulence` SVG data-URI at 3% opacity + `pointer-events:none`. (6) Google Maps `InfoWindow` keeps a white bg, so its `text-slate-800` text is CORRECT there — don't "fix" it to dark.

2026-06-03 | Pushed the Flip Analyzer to `main` expecting Cloudflare to auto-deploy the frontend. The CF Workers Build DID fire on push, but FAILED: build command `npx convex deploy --cmd 'npm run build'` → `401 Unauthorized: Invalid Convex deploy key`. The `CONVEX_DEPLOY_KEY` stored in **Cloudflare's build env** was stale (the prod deploy key had been rotated). A failed build = CF keeps serving the LAST good build, so the frontend silently stops updating with no error on the site. | The IRES Cloudflare Workers project **does deploy on `git push origin main`** and its build command is **`npx convex deploy --cmd 'npm run build'`** — i.e. the CF build deploys the **backend AND frontend** together (this supersedes the earlier "frontend = plain `npm run build` + manual backend deploy" note; someone enabled the `--cmd` wrapper). Therefore: (1) push triggers a real prod build — don't assume "pushed" = "deployed"; check Workers Builds. (2) The CF build needs a **valid prod `CONVEX_DEPLOY_KEY`** in Cloudflare → project → Settings → Variables/Build env. **After rotating the prod Convex deploy key, you MUST also update it in Cloudflare** (and `.env.local`), or every push 401s. The current valid prod key is in `.env.local` as `CONVEX_DEPLOY_KEY_PROD`. (3) Since the CF `--cmd` build already deploys the backend, a separate manual `npx convex deploy` is redundant once the CF key is valid (harmless, but not required). Fallback if the `--cmd` build later errors with "non-production build environment": switch the CF build command to plain `npm run build` + set `VITE_CONVEX_URL=https://pastel-crocodile-994.convex.cloud` in CF env (backend already deploys manually).

2026-06-03 | Built the Properties feature while ANOTHER session was concurrently building ARV-from-comps in the SAME working tree; it checked out its branch (`feat/arv-from-comps`) out from under me, so my commit landed on the wrong branch, and both features edited `convex/schema.ts` + regenerated `convex/_generated`. | Don't assume sole ownership of the working tree — check `git branch -vv` + reflog for unexpected checkouts/commits (telltale: recent commits you didn't make). Isolate with a worktree (`git worktree add <branch>` then the harness `EnterWorktree({path})`); copy `.env.local` + junction `node_modules` into it. **`npx convex codegen` AND `npx convex dev` both PUSH the local function set to the configured (shared) dev deployment — not offline.** To validate + regenerate `_generated` in isolation use the local anonymous backend: `$env:CONVEX_AGENT_MODE='anonymous'; npx convex dev --once` (separate local backend; writes nothing to `.env.local`). Give every implementer subagent a branch guard (verify `git branch --show-current` before committing). **Merge hazard:** when two branches both add to `schema.ts` + regenerate `_generated`, the SECOND to merge must regenerate `_generated` against the merged tree (anonymous backend) + verify `npm run build` — never hand-merge `api.js`/`api.d.ts` (a text merge silently breaks CI typecheck). Git's `ort` auto-merge of `api.d.ts` happened to be correct here, but regen is the rule.

2026-06-03 | Address autocomplete "didn't work" in prod: I built it on **Places API (New)** (`AutocompleteSuggestion.fetchAutocompleteSuggestions`), but the IRES Maps key only has the **legacy "Places API"** enabled, so the call returned nothing and the component silently fell back to free-text — no error, just no suggestions. | "Places API" and "Places API (New)" are SEPARATE Google Cloud APIs. The IRES key (`VITE_GOOGLE_MAPS_API_KEY` = `GOOGLE_GEOCODING_API_KEY`) has **legacy Places** — use the legacy `places.AutocompleteService.getPlacePredictions` (callback API; `componentRestrictions:{country:'us'}`, `types:['address']`, session token), the SAME family as Google Cloud's "Address Selection" console sample (legacy `new Autocomplete(input)`). Diagnose autocomplete failures by comparing the app key to the console-sample key (they matched here) and checking which API the working sample uses. (Supersedes the 2026-06-03 "Places API (New) enabled" note — it's legacy.) Browser-side; degrade to free-text when the lib is missing.

2026-06-03 | `git add -A` swept the repo's long-lived untracked artifacts (`.agents/`, `.claude/`, `_preview.png`, `skills-lock.json` — the shadcn-skill files) into a feature commit. | Stage explicit paths (`git add path1 path2 …`), not `git add -A`/`.`, in this repo until those artifacts are gitignored or deliberately committed. After a bad `git add -A`, `git reset --soft HEAD~1` + `git reset` + re-add only the intended files.

2026-06-03 | Native/Tailwind buttons default to `cursor: default`, so clickable buttons/cards didn't read as clickable. | Added a base-layer rule in `index.css` (`@layer base`): `button:not(:disabled), [role="button"], a[href], label[for], summary, select:not(:disabled), [data-slot="select-trigger"], [data-slot="dropdown-menu-trigger"] { cursor: pointer; }`. Base-layer < utilities, so explicit `cursor-*` classes still win. Prefer shadcn `Select` over plain `<select>` for themed dropdowns (one exception: a Radix Select inside a Google Maps `InfoWindow` portals its dropdown outside the InfoWindow, so an outside-click closes the InfoWindow — keep that one native).

2026-06-04 | User asked to install ReUI components (`@reui/c-dropdown-menu-1`, `@reui/c-combobox-1`). ReUI is built on **Base UI** (the `render`-prop API: `<DropdownMenuTrigger render={<Button/>}>`), but this repo's `components.json` is `base: radix` (`asChild`). The `@reui/c-*` items are **example blocks** (install to `components/examples/`); their real deps are the base components — and `npx shadcn@latest add @reui/c-combobox-1 --dry-run` showed it would **OVERWRITE `button.tsx` + `separator.tsx`** (used app-wide on Radix) with Base UI versions → would break the whole app. | Before installing ANY third-party shadcn registry item, **`--dry-run` (or `--view`) FIRST** and read the "overwrite" list — a registry on a different primitive base (Base UI vs Radix) silently clobbers shared `button`/`separator`/etc. Don't add a `@namespace` registry + install blindly. For a combobox/autocomplete this project already has a **Radix Popover + Command (cmdk)** pattern (`CandidateCombobox`→ now `src/web/PropertyPicker.tsx`, FlipAnalyzer picker) — reuse it (`shouldFilter={false}` + controlled `CommandInput value/onValueChange` to drive async sources like Google Places) instead of pulling in a second primitive system. Verify a component's base via `asChild` (radix) vs `render` (base) in its snippet (`.agents/skills/shadcn/rules/base-vs-radix.md`).

2026-06-04 | Tried to curl/Invoke-RestMethod FRED (and WebFetch its pages) from this machine to verify data freshness; every external HTTP call timed out / 403'd, even with the sandbox disabled. | **This dev sandbox blocks arbitrary outbound HTTP to external hosts** (FRED, etc.), but the **Convex CLI reaches Convex cloud fine** (`convex env set`/`dev`/`run` all worked). So: (1) don't validate an external API by curling it locally — it hangs; verify by running the **deployed Convex action** (`npx convex run xActions:fn`), which fetches from Convex's cloud (has network), then inspect with `npx convex data <table>`. (2) `WebSearch` (harness tool) still works for freshness signals when direct fetch is blocked. (3) For a serverless data-pull feature the constraint is local-only — the cron/action run in Convex cloud, unaffected. (4) FRED has BOTH a free instant API key (`api.stlouisfed.org/fred/series/observations`, JSON) AND a no-key CSV download (`fredgraph.csv?id=`); code both so the action degrades if the key is missing.

2026-06-04 | Built an additive dashboard feature while the tree already had **pre-existing uncommitted WIP** (property-zillow-facts) touching `convex/schema.ts`; my `marketMetrics` addition landed in the same already-modified file, and `convex dev --once` regenerated `convex/_generated` reflecting BOTH. | When the tree is dirty with someone else's WIP, a clean per-feature commit isn't just `git add` of your files — `schema.ts` + `_generated` are **entangled**. Either coordinate/sequence with the WIP owner, or stage your schema hunk with `git add -p` + your new files and **regenerate `_generated` to match exactly the schema you commit** (anonymous backend) so CI typechecks; never commit a `_generated` that reflects un-committed schema. When unsure, **don't auto-commit** — surface the entanglement and let the user sequence it. (Reinforces the 2026-06-03 "don't `git add -A`" + concurrency lessons.)

2026-06-04 | Security/robustness overhaul. Two reusable lessons: (1) **Convex redacts a plain `Error.message` to "Server Error" in production** — only a `ConvexError`'s `data.message` survives. `src/web/pages.tsx` showed `(e as Error).message`, so every prod scrape/geocode failure read "Error: Server Error" (looked like a broken CRM). Always surface caught errors via the `errMsg`/`describeError` helper (reads `err.data.message`), never `err.message`; throw `ConvexError({code,message})` (not `throw new Error`) for anything meant to reach the user. (2) **`requireUser` had an `IRES_DEV===1` → "dev-user" bypass** — env-gated + unset, but a latent full-auth-bypass if that flag were ever set; removed it (the dev-only `devScrapeSheriff/Legal` CLI actions keep their own independent `IRES_DEV` gate, so nothing broke). | Built an app-wide error system: `errorLogs` table + `convex/errors.ts` (admin-gated reads, `requireUser` writes, email stamped server-side), a branded `ErrorBoundary` ("contact your administrator", never a white screen), a global `window.error`/`unhandledrejection` best-effort logger (deduped + noise-filtered), a reusable branded `ConfirmDialog` replacing all native `window.confirm`, and an Admin → Error Log tab (resolve/clear) + unresolved-count sidebar badge. Verify a new Convex table's write path live with `npx convex run <internalFn>` + `npx convex data <table>` (the data dump bypasses function auth via the deploy key — handy when the public reader is admin-gated).

2026-06-06 | Evaluated Scrapling (open-source) vs Firecrawl for IRES scraping (isolated sandbox `C:\Users\nazho\Desktop\scraping-test`, 0 Firecrawl credits). | Findings: (a) the NCC parcel ASP.NET site (**Reblaze**) needs a REAL browser — a pure-HTTP POST of the ASP.NET search returns the empty page (Reblaze gates the search action behind a JS-set cookie); **Scrapling `StealthyFetcher` + `page_action` drives fill→search→details 3/3, free, ~15s**, owners matched the sheriff-PDF ground truth. (b) Zillow: Scrapling stealth gets past PerimeterX (resolves the right property, zestimate present) but ~69s + residential-IP-dependent → **keep Firecrawl for Zillow at scale** (managed proxies). (c) plain requests+BS4 = useless on protected sites. (d) A browser-based scraper needs a HOST/container → **conflicts with the serverless model**, so **prefer FREE HTTP APIs (ArcGIS) wherever possible**; introduce a browser worker only where a source forces it, and only against the small funnel. Full verdict: `scraping-test/output/ires/VERDICT-ires.md`.

2026-06-06 | NCC parcel data is a FREE ArcGIS REST layer — no need to scrape the Reblaze site for the SPINE. | `https://gis.nccde.org/agsserver/rest/services/BaseMaps/Base_Layers/MapServer/0` (Query + pagination, max 1000): `PRCLID` + situs + `PROPCLASS` + `LOTSZ` + **owner MAILING** (`OWNADDR*`, `OWNCITY/STATE/ZIP`, `CNTCTLAST`). So **spine + owner-mailing + ABSENTEE (owner mailing ≠ situs / OWNSTATE≠DE) = free for all ~200k, serverless, no browser.** NOT in it: full owner name, assessed value, sale, year built, tax/sewer balances, code violations (need other sources / per-parcel enrichment — mapped in the lead-engine Phase 0 Source Matrix). Thin layer `PW_PLL/PLL_Parcel_CRF/12` = PRCLID/ADDRESS/PROPCLASS/LOTSZ only; `LU_Eng` folder = token-secured.

2026-06-06 | Designing incremental ingestion at ~200k-parcel scale (the wholesaling lead engine). | (1) CDC key MUST be the natural key **`PRCLID`, NEVER the ArcGIS `OBJECTID`** — county truncate-and-reload ETL reassigns OIDs and would falsely flag all 200k as new. New parcels = paginated **PRCLID-only key-list diff** vs the stored set (cheap); a vanished PRCLID = split/merge → mark inactive (not an error). (2) **Never poll 200k for new signals** — subscribe to each source's dated feed (delta since a watermark) where one exists, else re-check ONLY the FUNNEL (flagged/watchlist/target-geo) on a tiered cadence. (3) Principle **ingest broad, surface narrow**: ingest every parcel cheaply, surface as a lead only once a distress signal attaches. Design: `docs/superpowers/specs/2026-06-06-wholesaling-lead-engine-design.md`. ⚠ this dev sandbox blocks outbound HTTP → verify ArcGIS/APIs via `WebFetch`/`WebSearch` or a deployed Convex action, not local curl.

2026-06-06 | Phase 0 research for the wholesaling lead engine — needed to verify external ArcGIS endpoints' shape/data, but this dev sandbox blocks local outbound HTTP. | **Probe external APIs with a THROWAWAY Convex `internalAction` on the CLOUD dev deployment** (`fearless-donkey-585`), run via `npx convex run probe:fn` — the cloud has network; the local machine (and the anonymous *local* backend) do NOT. Make it args-driven (`get({urls})`, `layer({layerUrl})`) so you explore many endpoints WITHOUT redeploying each time; return small parsed shapes (field names, counts, samples) not raw blobs (ArcGIS layer-metadata JSON > 4 KB truncates). **Delete the probe + `npx convex dev --once` to re-sync dev clean afterward** (never commit it). ArcGIS REST returns errors as **HTTP 200 with `{"error":{...}}`** — a `res.ok`-only check passes them; inspect the body.

2026-06-06 | Probing the NCC ArcGIS spine: paginating a single narrow field 400'd. | `…/query?where=1=1&outFields=PRCLID&resultRecordCount=1000&resultOffset=0` returns **`error 400 "Failed to execute query"`** UNLESS you add **`orderByFields=PRCLID`** (a single-field page needs an explicit stable sort to paginate; `outFields=*` pages fine without it). The recurring `PRCLID`-only CDC key-diff MUST include `orderByFields`. Spine facts (verified live): `BaseMaps/Base_Layers/MapServer/0` = **203,752** parcels; 1000-key page ≈ 39 KB (full key list ≈ 8 MB, cheap); 1000 full rows ≈ 820 KB (full seed ≈ 167 MB); only `TAXAREA` is value-ish (no assessed value/sale/year-built). FirstMap `PlanningCadastre/DE_StateParcels` is thinner (`PIN/ACRES/COUNTY/UPDATED` only — no owner/value).

2026-06-06 | Assumed (in the spec) that NCC distress signals would mostly need browser scraping / be per-parcel-only. Phase 0 found the opposite. | The NCC ArcGIS **`CustomMaps` folder** (`https://gis.nccde.org/agsserver/rest/services/CustomMaps`) is a free, public, `PRCLID`-keyed suite of distress feeds — **always enumerate ALL folders/services of a GIS server before concluding a signal needs scraping.** Found: `CodeEnforcement_CodeCases/0` (2,852 cases; **dated delta VERIFIED** — `where=APDTTM > TIMESTAMP 'YYYY-MM-DD HH:MM:SS'` returned 748 since 2026-05-01; ⚠ `last_edited_date` is sparse → 0, and an **epoch-ms `where` 400s** — use the `TIMESTAMP '…'` literal) + `Code_Enforcement/MapServer` views (6 Vacant Properties=859, 9 Open Cases, 11 "Cases added last 30 days"=1,051, 8 Resolved-with-Fees-Owed); `SheriffSales/0` (53, structured `CASENUMBER`/`PLANTIFF` — can replace the brittle sheriff-PDF parse) + `SheriffSales/1 Vacant Monitions Candidates` (76, curated vacant+tax-delinquent); `RentalUnits/0` (39,424, `EXPDATE`); `Permits/4 New Construction`; `Ownership/0` (203,752; `CNTCTLAST` holds the **full owner-name string**). `LU_Eng`/`PublicWorks` folders = token-secured (499). Net: code violations + vacancy + rentals + structured sheriff sales are FREE/serverless; only **assessed value + tax/sewer balances** (Reblaze per-parcel site) and **upstream court lis-pendens** (CourtConnect) still need browser/paid. Full matrix: `memory/source-matrix.md`.

2026-06-07 | Seeding all 203,752 NCC parcels from the ArcGIS layer kept dying at the EXACT same record (~PRCLID 1100830074) with "ArcGIS 400: Failed to execute query" — across offset paging AND keyset paging. | Root cause (found by binary-searching the region from a throwaway cloud-dev probe): **`outFields=*` 400s on a multi-record page in a dense region because one of the layer's ~42 fields (one we don't use) holds a corrupt/oversized value.** Proof: `PRCLID = '<id>'` (equality) + `*` works; `PRCLID > '<id>'` + `*` fails even for 1 row; keys-only works; **an explicit field list works even at 1000 rows** (366 KB). **FIX: never request `outFields=*` — list only the fields you parse** (`PARCEL_FIELDS` in `arcgisParcels.ts`); smaller + dodges the bad field. Also: deep `resultOffset` paging is fragile at scale (intermittent 400s) → use **keyset pagination** (`where PRCLID > cursor` + `orderByFields=PRCLID`); single narrow-field paging needs the orderBy or it 400s. Belt-and-suspenders kept: adaptive page-halving on 400 + transient retry w/ backoff. Verified: full 203,752 seed, 53,293 absentee (26%), idempotent. Diagnose ArcGIS by isolating equality-vs-range-vs-keys-only and field-list-vs-`*` from a cloud probe (local HTTP is sandbox-blocked).

2026-06-07 | Renamed a Convex schema field (`parcelSync.offset` → `cursor`); `convex dev --once` refused to push because an existing (failed-seed) row still had `offset`, which the new validator rejects (chicken-and-egg: can't deploy a cleanup mutation without first pushing the schema). | Clear the stale rows with **`npx convex import --table <t> --replace empty.jsonl --yes`** (an empty file) — import validates against the CURRENTLY-deployed schema, so it bypasses the new-schema block — THEN push the new schema. Same trick wipes a table for a clean re-seed. (Dev throwaway data; for prod data a real migration mutation would be needed.)

2026-06-07 | Built the parcel spine (203k rows) on Convex. Two reusable patterns. | (1) **Counters, not COUNT:** Convex can't cheaply count ~203k docs (query doc/bandwidth limits), so the seed maintains running counters (`processed/inserted/absentee`) on a `parcelSync` progress row; `parcelStats` reads that row (and reads the latest **seed** run for totals — a "sync" run only records deltas, so reading it for totals shows ~0). (2) **Resumable self-rescheduling action:** seed/sync paginate one keyset page, upsert in 250-row chunks (txn limits), then `ctx.scheduler.runAfter(0, self, {afterPrclid})`; CDC sync is a cheap keys-only key-diff per range (new in / vanished out — NOT a full re-pull); attribute changes are refreshed by re-running the heavier full `seedSpine`.

2026-06-08 | Debugged the 203k-parcel seed by re-running the FULL seed ~4× (plus several full syncs) on the Convex **free tier** (dev) → exhausted the monthly quota; the user "can't do anything anymore" on Convex. | **When iterating on a mass-write/mass-fetch job, debug on a SMALL bounded subset** (cap to a few pages / one PRCLID region), prove it, THEN run the full job ONCE. One full parcel seed ≈ 204 page-fetches + ~203k writes + ~800 mutation calls; doing that repeatedly blew the free-tier monthly **function-call / bandwidth / action-compute** limits (storage of ~203k docs is fine, well under 1 GB). **Steady state is cheap** — the weekly keys-only CDC key-diff is near-zero writes; only the repeated FULL re-seeds cost. Mitigations: (1) add a `maxPages`/`limit` arg to `seedSpine` so debugging can't accidentally full-seed (do this FIRST next session); (2) for prod, seed **once** off-peak; (3) Convex monthly limits reset on the billing cycle — or clear the dev `parcels` table to reclaim storage if needed. **Don't run ANY Convex ops while over quota** (they add to usage); documentation/build/tests are file-based and safe.

## Carried over from the original Python/n8n pipeline (still true)
- **Zillow:** scrape the search URL (`/homes/ADDRESS_rb/`) with markdown — Firecrawl renders the homedetails page. Validate `-DE-` is in the resulting URL to reject wrong-state matches. Homedetails URLs scraped directly return 403.
- **NCC parcel site:** needs Firecrawl **browser actions** (click/write/click) — bypasses Reblaze bot protection that blocks plain requests from non-local IPs.
- **Address cleaning** is essential: truncated zips, missing spaces, AKA suffixes, `ZIP_ONLY` fallback to the parcel's address.
- **Python launcher on this Windows machine is `py`** (not `python`/`python3`) — for any Python tooling.

2026-06-11 | The PROD parcel seed stalled silently at exactly 132k: a hung external fetch (no response, no error) ran the action into its time limit, which KILLS it without throwing — so the try/catch never finalized the run, the self-rescheduling chain died, and the sync row sat "running" forever with no error logged. | Every external `fetch` in a Convex action MUST set `signal: AbortSignal.timeout(30_000)` — a timeout converts a hang into a normal throw that retries/halving can handle. Diagnose a stalled self-rescheduling chain by polling the progress row (`statsInternal`): frozen counter + status "running" = killed chain; RESUME with the stored cursor + syncId (`npx convex run parcelActions:seedSpine '{"syncId":"…","afterPrclid":"<cursor>"}'`) — that's what the resumable design is for.

2026-06-11 | `npx convex run` on a LONG-running action (the ~3-min CourtConnect sweep, the multi-minute seed) reported "✖ Failed to run function … Error" while the action COMPLETED successfully server-side (watermark/progress rows proved it) — twice nearly caused a false-failure diagnosis. | The Convex CLI times out client-side on long actions; its exit code is NOT the action's outcome. Verify long actions via their observability rows (`signalData:signalStatsInternal`, `parcelData:statsInternal` — every long action should WRITE one), or run them in background and poll. (Extends the 2026-06-01 "trust the captured output" lesson.)

2026-06-11 | The first prod foreclosure sweep had 5/32 lender stems fail transiently, but still advanced the watermark to "now" — those stems' filings inside the window would have been PERMANENTLY skipped (the date-filtered search never revisits old filing dates). | A watermark that tolerates partial per-item failures must NOT advance past them: on any partial failure keep the watermark at the window START (idempotent upserts make the re-sweep free). Recovery for an already-advanced watermark: rewind it via the internal `setWatermark` mutation and re-run (also how to force a full re-pull).

(NOTE: 2026-06-13 P5 lessons live on branch `feat/p5-contacts-skiptrace`; the ones below were appended on `feat/p6-offers-contracts`. Reconcile both lesson sets when the branches merge.)

2026-06-14 | (SECURITY) Building the public e-sign portal: `contracts.publicToken` is a `v.optional` field AND indexed (`by_token`). In Convex, a row with the field UNSET is indexed as `undefined`, so `q.eq("publicToken", undefined)` MATCHES every tokenless (draft/voided) row — i.e. an empty/undefined token in a token-as-credential lookup would silently return a real row = an auth bypass. | When a SECRET token is the ONLY credential for a public/unauthenticated function, guard the lookup at TWO levels: (1) reject falsy input before the query (`if (!token) return null`), and (2) after `.first()`, re-check the matched row actually carries the field (`return row?.publicToken ? row : null`). `v.string()` arg validation helps but don't rely on it alone — the index-match-on-undefined is a latent silent bypass a refactor could reopen. Route ALL token lookups through one private helper so there's a single audited chokepoint.

2026-06-14 | Two feature branches (`feat/p5-contacts-skiptrace`, `feat/p6-offers-contracts`) both branch off `main` and both add tables to `schema.ts`. Running `npx convex dev --once` from one branch reshapes the SHARED dev deployment to that branch's schema — it reported DELETING the other branch's table indexes (e.g. P6's push dropped `contacts.by_prclid`). | This is expected with one shared dev deployment + divergent branch schemas; it affects only dev DEPLOYMENT STATE, not either branch's code/commits (the dropped tables had no data of consequence). It re-pushes when you switch back to that branch and run `convex dev --once`. Don't panic at "Deleted table indexes". MERGE hazard (the real risk): when the SECOND branch merges to main, regenerate `convex/_generated` against the merged `schema.ts` (anonymous backend or `convex dev --once`) + `npm run build` — never hand-merge `api.*`. Memory docs (todo/lessons/next-session) also diverge across the two branches → reconcile (append both lesson sets) at merge.

2026-06-14 | `@react-pdf/renderer` component typed `: JSX.Element` failed to build under React 19 with `TS2503: Cannot find namespace 'JSX'` — React 19 removed the global `JSX` namespace. | Add `import type { JSX } from "react"` (React 19 re-exports `JSX` as a named type) when a file annotates `JSX.Element`, or use `React.ReactElement`. General: this repo is on React 19 — the global `JSX.*` namespace is gone; import it from `react` or avoid it.

2026-06-21 | Researching the cheapest vision LLM for Street-View condition scoring (P7); the user named "GLM 5.2" and DeepSeek. | (a) The user's "GLM 5.2" (`z-ai/glm-5.2`) is **TEXT-ONLY** — it cannot accept an image; the real Zhipu vision model is **GLM-4.6V**. **DeepSeek has NO usable vision model** (V4 is text-only; DeepSeek-VL2 is old/small/not on OpenRouter). Don't assume a model name implies vision — verify modality on openrouter.ai/models. (b) At a few-houses-at-a-time volume, vision-model cost is negligible (~$0.18–$1.40 per 1,000 images across Gemini Flash-Lite/Flash, GLM-4.6V, Qwen3-VL, Haiku) → choose on **vision reliability + structured-output cleanliness**, not price. Picked **`google/gemini-2.5-flash` via OpenRouter** (best calibration + native image→JSON), env-swappable (`CONDITION_LLM_MODEL`) to GLM-4.6V/Qwen3-VL for A/B. (c) OpenRouter bills images as input tokens, no per-request surcharge; send the image as **base64** to keep the Maps key server-side. (d) Use prompt-instructed JSON + a tolerant fence-stripping parser (like `legalNotices.ts`) rather than per-model `json_schema`, so all A/B models behave identically.

2026-06-21 | A plan's example code used `describeError(err)` as a string, but `describeError` (`src/web/lib/errorReporting.ts`) returns `{message, expected}`, not a string. | Plan/spec example code is a STARTING POINT, not verified. The implementer subagent correctly fixed it to `describeError(err).message` (matching every `LeadsPage.tsx` call site) and flagged it in its report. When transcribing plan code that calls an existing helper, verify the helper's real return type against its definition — don't trust the plan's usage. (This is exactly what the per-task review loop is for.)

2026-06-21 | Researching the cheapest vision LLM for Street-View condition scoring (P7); the user named "GLM 5.2" and DeepSeek. | (a) The user's "GLM 5.2" (`z-ai/glm-5.2`) is **TEXT-ONLY** — it cannot accept an image; the real Zhipu vision model is **GLM-4.6V**. **DeepSeek has NO usable vision model** (V4 is text-only; DeepSeek-VL2 is old/small/not on OpenRouter). Don't assume a model name implies vision — verify modality on openrouter.ai/models. (b) At a few-houses-at-a-time volume, vision-model cost is negligible (~$0.18–$1.40 per 1,000 images across Gemini Flash-Lite/Flash, GLM-4.6V, Qwen3-VL, Haiku) → choose on **vision reliability + structured-output cleanliness**, not price. Picked **`google/gemini-2.5-flash` via OpenRouter** (best calibration + native image→JSON), env-swappable (`CONDITION_LLM_MODEL`) to GLM-4.6V/Qwen3-VL for A/B. (c) OpenRouter bills images as input tokens, no per-request surcharge; send the image as **base64** to keep the Maps key server-side. (d) Use prompt-instructed JSON + a tolerant fence-stripping parser (like `legalNotices.ts`) rather than per-model `json_schema`, so all A/B models behave identically.

2026-06-21 | A plan's example code used `describeError(err)` as a string, but `describeError` (`src/web/lib/errorReporting.ts`) returns `{message, expected}`, not a string. | Plan/spec example code is a STARTING POINT, not verified. The implementer subagent correctly fixed it to `describeError(err).message` (matching every `LeadsPage.tsx` call site) and flagged it in its report. When transcribing plan code that calls an existing helper, verify the helper's real return type against its definition — don't trust the plan's usage. (This is exactly what the per-task review loop is for.)

2026-06-26 | P7 `/condition` "Score condition" did nothing on PROD — every click stored hasImagery:false + the Gemini badge ("No Street View coverage"), but Gemini was NEVER called. | ROOT CAUSE: the **Street View Static API was not ENABLED** on the Google Cloud project that owns the Maps key (`GOOGLE_GEOCODING_API_KEY` = `VITE_GOOGLE_MAPS_API_KEY` = `AIzaSyB0w_…`, SAME key dev+prod; Geocoding WAS enabled, Street View Static never was — the long-unverified punch-list item). The Street View **metadata** endpoint returns **HTTP 200** with `{"status":"REQUEST_DENIED","error_message":"This API is not activated on your API project."}` and the **image** endpoint returns **403** same text — but `conditionActions` lumped every non-OK metadata status into `no_imagery` (silent), the exact "surface hard config errors, don't no-op" anti-pattern. TWO Google gotchas: (1) **"Selected APIs" (a KEY's API-restriction list) ≠ "Enabled APIs" (APIs & Services → Enabled APIs for the PROJECT)** — an API can be selected on the key yet not enabled on the project; "not activated on your API project" is PROJECT-level → enable it via the API Library for THAT key's project. (2) Right after enabling, the billable **image** endpoint 403'd intermittently for ~1 min (Google enablement **propagation lag**: metadata went OK immediately, image lagged) then went consistent — retry briefly before concluding failure. FIX shipped (commit c430470, LOCAL-only): pure `classifyStreetViewMetadata` (OK→ok · ZERO_RESULTS/NOT_FOUND→no_imagery · else→error surfacing `error_message`) wired into the coverage check + 6 tests (218 total, build clean). Verified end-to-end via the throwaway cloud-Convex probe (local HTTP sandbox-blocked): metadata OK + real JPEGs (96–120 KB) + Gemini score 20. NOTE: a probe internalAction that calls `internal.*` NEEDS an explicit `: Promise<...>` return annotation or TS7022/7023 cascades and poisons sibling files (parcelActions) until annotated.

2026-06-27 | P7 v2 condition-batch: score the top-100 houses' exterior condition via Claude vision (no Gemini) + get the image bytes into Convex. | (a) **`npx convex run` takes ONLY an inline JSON arg (no file/stdin) and Windows caps a CLI arg at ~32 KB** — so base64-image-in-a-CLI-arg is impossible (even a 400px JPEG ~73 KB exceeds it). Convex `_storage` transport = the **upload-URL flow**: `generateConditionUploadUrl` (internalMutation → `ctx.storage.generateUploadUrl()`) → `curl -X POST "$url" --data-binary @img.jpg` → storageId → pass the tiny storageId via CLI to the record mutation. **convex.cloud HTTP IS reachable from this sandbox** (the FRED-style external-HTTP block does NOT apply to convex.cloud — verified `HTTP 200`). (b) **Claude IS the vision model** — a subagent's `Read` on a downloaded Street View JPG scores it; ZERO per-image LLM API cost. Scale 100 houses with **parallel background `opus` subagents** (one per ~12-house `split` slice; each Read→score→upload→record), ~4 min/slice, context-lean (never view 100 images in the main loop). (c) **No false info** (the product bar): route the skill's writes through the SAME `parseConditionResponse` sanitizer SERVER-SIDE (`recordConditionScore` takes the raw model JSON, clamps score 0-100 / filters flags to the closed set / validates confidence, and stores a visible `lastError` on unparseable output — never a fabricated score). A gray <9 KB Street View image = Google "no imagery" placeholder → `hasImagery:false`, no guessed score. (d) Build the record payload with `node -e 'JSON.stringify(...)'` from a `cat`'d raw-JSON file so a `'`/`"`/newline in the description can't break the shell. Run result: 100/100 (86 scored, 14 no-coverage, 0 fabricated, 0 write errors).

2026-06-27 | Shipped a feature to `main` while a concurrent Claude session ran in the SAME repo (it made `feat/command-center`/`feat/hardening`, switched the shared working tree, then merged its work into `main`). | Isolate in a worktree (`git worktree add .claude/worktrees/<x> <branch>` + harness `EnterWorktree`; junction `node_modules`, copy `.env.local`, codegen with `CONVEX_AGENT_MODE=anonymous` so it never reshapes shared dev). Land commits on `main` WITHOUT disrupting the other session: while `main` is NOT checked out anywhere, **ff its ref** (`git branch -f main HEAD` + `git push origin main`); once the other worktree has `main` checked out, `git branch -f main` FAILS ("cannot force update the branch 'main' used by worktree …") → instead `git rebase origin/main` then **`git push origin HEAD:main`** — a REMOTE fast-forward that updates origin/main without touching the other worktree's local checkout (their `main` catches up on their next pull). Both features coexisted (mine was an ancestor of their merge, so it survived). Key distinction: **pushing to remote `main` ≠ updating the LOCAL `main` ref** — the former is allowed under a foreign checkout, the latter is blocked. Stash your own uncommitted memory edits before the other session's `git add -A` sweeps them.

2026-07-01 | Task 11 headless preview of `/monitor`: first screenshot (`chrome --headless=new --screenshot` on a `file://` URL of the built preview) was a BLANK white page. | A `<script type="module">` entry does NOT execute over `file://` (the browser blocks cross-origin module fetches), so the app never mounts — this refines the 2026-06-03 headless method, whose "file URL" note only holds for a NON-module inline bundle. Fix: SERVE the built preview over HTTP and screenshot that — `npx vite preview --config vite.preview.config.ts --port <p>` then shoot `http://localhost:<p>/preview.html`. Also two clean wins for the mock: alias `convex/react` → a stub in a THROWAWAY `vite.preview.config.ts` (no source edits) and switch the stub's `useQuery` on `getFunctionName(ref)` (returns `"monitorData:listKeepers"` etc.) to return per-query sample rows; render inside a minimal `createMemoryHistory({initialEntries:["/monitor"]})` router with stub `/flip`+`/potential` routes so the page's `<Link>`s resolve without the real AppShell/Clerk. Commit the real work FIRST so the throwaway harness is all-untracked and `rm`-able (no `git checkout -- .` that could nuke real edits). SEPARATELY: adding a public FN to an EXISTING convex module needs NO `_generated` change either — `api.d.ts` references `typeof monitorData` (the whole module), so `tsc` picks up `markPromoted` straight from `monitorData.ts`; codegen's only diff was LF→CRLF churn (reverted).

2026-07-01 | Task 7 (schema) ran in the worktree `…/monitor-web`, but my first Read/Edit used the MAIN-repo path `C:\Users\nazho\Desktop\ires-crm\convex\schema.ts` (branch `main`) — so the edit landed in the wrong working tree and the worktree's `git status` showed schema.ts UNCHANGED. Caught it because schema.ts was absent from the worktree diff (only spurious `_generated` LF/CRLF churn showed). | In a git worktree, ALWAYS build file paths from the worktree root (`git rev-parse --show-toplevel`), never the bare repo root — the two repos share a branch each and edits don't cross over. Recovery: `git checkout -- <file>` in the main repo to revert the stray edit, re-apply to the worktree copy, re-run codegen/build there. Also: adding a Convex TABLE needs NO `_generated` change — `dataModel.d.ts` derives `DataModel = DataModelFromSchemaDefinition<typeof schema>` generically, so `tsc` picks up new tables straight from `schema.ts`; the CLI's only `_generated` diff was LF→CRLF churn (revert it, commit `schema.ts` alone).

2026-07-01 | Mid-run the Claude process RESTARTED (session id `40674cfd`→`40af4b9d`) and a FORKED duplicate of the controller kept running — a persistent "twin" that autonomously executed the SAME subagent-driven plan in the SAME worktree (committed Tasks 2–15 + its own reviews/fixes/memory), resurrecting after every lull (my "idle confirmed" check caught only a temporary gap). | Two controllers on one branch WILL collide + diverge on non-deterministic tasks; racing wastes tokens (a dispatched agent hit the live twin's in-flight edits, ran 19 min, made a redundant fix commit). DETECT via CONTROLLER artifacts you didn't create (`review-*.diff`, `task-N-brief.md`, commits/uncommitted edits appearing between your turns). Safe play when you can't kill the twin (separate process): go fully HANDS-OFF — no dispatch, no commit — so there's a SINGLE clean commit lineage (the only thing that corrupts it is your OWN dispatch); MONITOR to completion; then take over for the final review + deploy. Background watcher bash jobs get KILLED by the same process-lifecycle events → use a SELF-HEALING relaunch loop (each kill notifies you → re-check git + relaunch); settle-detect on `git rev-parse HEAD` stable + no `find -mmin` file activity for N min (the twin also writes the shared `.superpowers/sdd/progress.md`, so watch mtimes broadly). GROUND TRUTH = git + the ledger, never in-context recollection. The twin stopped before merging to `main` (finish-branch awaits user), so `main` stayed clean for the live session to own the deploy.

2026-07-01 | User: skip dev, deploy Monitor straight to PROD + pen-test the LIVE CRM (to conserve Convex). | Deploy the backend from the WORKTREE: `CONVEX_DEPLOY_KEY=<prod> npx convex deploy -y` (adds tables/indexes/functions/http-route/cron to `pastel-crocodile-994`). Merge = `git push origin HEAD:main` (REMOTE ff — `main` was checked out in another worktree) → triggers the Cloudflare frontend build. Set `FIRECRAWL_WEBHOOK_SECRET` on prod (`convex env set`, prod key); FIRECRAWL/OPENROUTER already present. Trigger a live scan via the INTERNAL action with the deploy key (`convex run monitorActions:runMonitorScan '{"trigger":"manual","maxPages":1}'`) — it returns after SCHEDULING; the `analyzeOne` fan-out finishes ASYNC (poll `npx convex data monitorListings`, count status pending→analyzed). Inspect surfaced deals via an INTERNAL query (`keepersToEmail`, pipe to `node -e` for a table) because the public `listKeepers` is `requireUser`-gated → CLI returns UNAUTHENTICATED (which itself CONFIRMS the auth gate). Security-pen-test the public webhook by curling `https://<prod>.convex.site/firecrawl-monitor` (convex.site IS reachable from this box): no-sig / bad-sig → 401, GET / nonexistent route → 404 (fails closed). The UI + Promote pen-test needs a SIGNED-IN browser and I CANNOT enter credentials — use claude-in-chrome `list_connected_browsers`→`switch_browser` to the user's logged-in Chrome (they complete Clerk sign-in). Result: 41→24→24 analyzed / 0 failed, real investor-grade insights, `/monitor` UI clean, Promote→Potential works. (Screenshots on the Clerk redirect intermittently CDP-timeout; `read_page` still works — use it to read state when a screenshot hangs.)
